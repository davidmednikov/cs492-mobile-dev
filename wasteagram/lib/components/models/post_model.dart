class PostModel {

  String id;
  DateTime date;
  String imageURL;
  int quantity;
  double latitude;
  double longitude;

  PostModel({ this.id, this.date, this.imageURL, this.quantity, this.latitude, this.longitude });
}